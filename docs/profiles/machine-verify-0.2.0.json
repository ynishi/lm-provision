{
  "type": "Spec",
  "name": "machine-verify",
  "version": "0.2.0",
  "description": "Smoke-test a freshly acquired GPU machine. Declares the machine in the requirement slots (image, GPU, disk, ports) so `acquire` can obtain one and `check` can judge one, then runs a single nvidia-smi to prove the GPU is reachable. Use this as the first profile against any new pod or provider account.",
  "capabilities": [
    "sh.exec"
  ],
  "paths": [
    "/workspace"
  ],
  "http_allowlist": [],
  "requires_ports": {
    "8188": "public_http",
    "22": "raw_tcp"
  },
  "requires_gpu": {
    "count": "1",
    "min_vram_gb": "40"
  },
  "requires_disk": {
    "ephemeral_gb": "60",
    "persistent_gb": "80",
    "persistent_at": "/workspace"
  },
  "provider": {
    "runpod.imageName": "runpod/pytorch:2.4.0-py3.11-cuda12.4.1-devel-ubuntu22.04"
  },
  "phases": [
    {
      "type": "ShExec",
      "argv": [
        "sh",
        "-c",
        "nvidia-smi --query-gpu=name,memory.total --format=csv,noheader"
      ]
    }
  ]
}
